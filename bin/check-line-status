#!/usr/bin/env ruby

["/", "app/service"].each do |folder|
  path = File.expand_path(File.join(File.dirname(__FILE__), "../", folder))
  $LOAD_PATH << path
end

require "mongo_mapper"

require "naantala_service"
Naantala::Service::LineStatus.check_status
